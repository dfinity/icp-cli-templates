name: Test templates

on:
  pull_request:
  push:
    branches: [main]

jobs:
  test:
    name: Render, build, deploy and verify templates
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run template tests
        run: make test
